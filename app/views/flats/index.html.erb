<h1>Flats</h1>

<ul>

  <% @flats.each do |flat| %>
    <li>
      <h2><%= flat["name"] %></h2>
      <p>price: <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
      <%= link_to "Plus d'info", flat_path(flat["id"])  %>
    </li>
  <% end %>

</ul>
